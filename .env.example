# Infinity XOS Global Index - Environment Configuration

# Server Configuration
PORT=3000
NODE_ENV=development

# CORS Configuration
# Comma-separated list of allowed origins for Cross-Origin Resource Sharing
# For production, specify only trusted domains
# For development, defaults to common localhost ports
# Examples:
#   Production: ALLOWED_ORIGINS=https://app.example.com,https://admin.example.com
#   Development: ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001,http://localhost:8080
